import { createContext } from "react";

const contextfirst=createContext()

export   const tasknine=createContext()

export const themecontext=createContext()

export const fetchcontext=createContext()

export const loginlogoutcontext=createContext("Loggedin")
export const languagecontext=createContext("language content")

export default contextfirst

