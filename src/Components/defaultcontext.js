import { createContext } from "react";

const contextdefault=createContext("hii this is default context value passed without using provider")

export const contexttwo=createContext("contexttwo")

export default contextdefault

